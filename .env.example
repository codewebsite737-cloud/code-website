# Server-only. Never expose this value in browser code or commit a real key.
OPENROUTER_API_KEY=

# Optional. Defaults to the zero-cost OpenRouter free-model router.
OPENROUTER_MODEL=openrouter/free

# Optional authenticated-user generation quota. Defaults to 20 per day.
AI_DAILY_LIMIT=20
